#!/bin/bash

TARGETDIR="$1"

#cp -a "$HOST_DIR/usr/bin/grub"* "$TARGETDIR/usr/bin/"
#cp -a "$HOST_DIR/usr/sbin/grub"* "$TARGETDIR/usr/sbin/"
#for D in /usr/share/info/grub.info /usr/share/info/grub-dev.info /usr/share/grub /usr/lib/grub /etc/bash_completion.d/grub /etc/grub.d; do
#    cp -a "$HOST_DIR$D" "$TARGETDIR$D"
#done


